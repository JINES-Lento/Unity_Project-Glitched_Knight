//주인공의 공격 매커니즘을 구현하는 스크립트
using UnityEngine;

public class CharacterAttack : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
